Скрипт снимает free -h, df -h, uptime  раз в секунду и записывает в monitor.log с временной меткой в начале. При запуске с аргументом `-b` (`./script.sh -b`) работает в фоновом режиме, выводя свой PID в stdout
